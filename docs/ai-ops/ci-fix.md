# CI 修复记录（ai-ops 结构化知识沉淀）

> autofix 脚本自动回写（tools/ai-ops.py），每行 = 一次修复闭环，同一 run 只记一次。
> 完整会话报告（错误原因+修复过程）见仓库根 `log/ai-ops/{date}-{project}.md`（不入库）。
> 人工提交修复后，可将对应行「状态」列由「待提交」更新为「已提交(<hash>)」（docs/07 风格）。

| 日期 | 项目 | run | 问题 | 修复 | 状态 |
|------|------|-----|------|------|------|