# library/debian/config.yml
project:
  org: library
  name: debian

version_source:
  type: script
  script: get_latest_version.sh

variants:
  - name: "standard"
    template: "Dockerfile-standard.template"
    description: "Standard (full)"
  - name: "slim"
    template: "Dockerfile-slim.template"
    description: "Minimal"

push:
  registry: "lcr.loongnix.cn"
  repository: "library/debian"
